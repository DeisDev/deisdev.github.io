{
  "name": "deisdev-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "node scripts/sync.mjs",
    "check": "astro check && node --check scripts/sync.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "astro": "7.3.3",
    "simple-icons": "15.22.0",
    "typescript": "6.0.3"
  }
}
